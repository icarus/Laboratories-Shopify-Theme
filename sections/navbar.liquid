<nav class="navbar">
  <a class="navbar-logo" href="{{ shop.url }}">
    <p>Laboratories</p>
  </a>
  <div class="navbar-menu">
    <ul class="navbar-nav">
      {% for link in linklists.main-menu.links %}
        <li class="nav-item">
          <a class="nav-link" href="{{ link.url }}">{{ link.title }}</a>
        </li>
      {% endfor %}
      <a class="last-nav-item js-cart-link">
        <span class="nav-link js-cart-item-count">Cart</span>
        <span class="cart-count js-cart-item-count">{{ cart.item_count }}</span>
      </a>
    </ul>
  </div>
</nav>
