<nav class="navbar">
  <a class="navbar-logo" href="{{ shop.url }}">
    <p>Laboratories</p>
  </a>
  <div class="navbar-menu">
    <ul class="navbar-nav">
      {% for link in linklists.main-menu.links %}
        <li class="nav-item">
          <a class="nav-link" href="{{ link.url }}">{{ link.title }}</a>
        </li>
      {% endfor %}
      <li class="last-nav-item">
        <a class="nav-link js-cart-item-count js-cart-link">Cart ({{ cart.item_count }})</a>
      </li>
    </ul>
  </div>
</nav>
