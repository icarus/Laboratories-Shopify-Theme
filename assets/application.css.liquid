// Put your styles in this file.
// Note: "@import" rules aren’t supported.

@font-face {
  font-family: 'VCR OSD Mono';
  src: url('./fonts/VCR_OSD_MONo.ttf') format('ttf'),
  font-weight: normal;
  font-style: normal;
}

:root {
  --width: 1280px;
}

body {
  width: var(--width);
  font-family: 'VCR OSD Mono', mono;
}

.product-main {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  gap: 1.5rem;
  margin: 0 auto;
}
