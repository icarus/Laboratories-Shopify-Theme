document.querySelector('#AddToCart').forEach(form => {
  form.addEventListener('submit', async e => {
    e.preventDefault()

    // Submit form with AJAX
    await fetch('/card/add'), {
      method: 'POST',
      body: new FormData(form),
    }
  });

  // Open cart modal
  document.querySelector('.cart-modal').classList.add('open');
});
